version: "3.2"

services:
  backend:
    container_name: makemegod
    image: kvasbo/make_me_god:latest
    ports:
      - "80:80"
    volumes:
      - ./bibles:/makemegod/bibles
networks:
  default:
      name: ${NETWORK_NAME}
      external: true
